# Production probe — 2026-07-24

**Target:** `https://margn.margnhq.workers.dev`  
**Probe origin:** Sako's network, separate from the deployment machine  
**Result:** healthy

## Repeated route checks

Each production route was called four times after an initial validation pass.

| Route | Runs | HTTP result | Latency range |
| --- | ---: | --- | ---: |
| `POST /v1/quote` | 4 | all `200` | 110–300 ms |
| `POST /v1/verify` | 4 | all `200` | 272–323 ms |
| `POST /v1/check` | 4 | all `200` | 276–307 ms |

No response contained an HTTP 500 status or an unhandled server exception.

## Failure handling

| Input | Result |
| --- | --- |
| unknown `agentId` | HTTP 200 with `AGENT_NOT_FOUND` JSON |
| empty `need` | HTTP 200 with `INVALID_NEED` JSON |
| malformed JSON | HTTP 200 with `INVALID_JSON` JSON |

The valid `verify` probe for agent `3152` reached its upstream endpoint, which
returned HTTP 403. Margn correctly described that upstream as `suspicious`
while keeping its own response healthy and structured.

## Clean-checkout verification

From `endpoint/`:

- `npm ci` installed 98 packages successfully from the committed lockfile.
- `npm run check` passed.
- `npm test` passed: 46 tests across 2 files.
- `npm audit` reported 0 vulnerabilities.
